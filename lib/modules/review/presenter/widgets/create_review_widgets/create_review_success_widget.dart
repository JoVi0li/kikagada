import 'package:flutter/material.dart';

class CreateReviewSuccessWidget extends StatelessWidget {
  const CreateReviewSuccessWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}