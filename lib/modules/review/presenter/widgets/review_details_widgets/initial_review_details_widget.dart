import 'package:flutter/material.dart';

class InitialReviewDetailsWidget extends StatelessWidget {
  const InitialReviewDetailsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
